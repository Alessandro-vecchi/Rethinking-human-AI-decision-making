# Python 3.11+. Pin exact versions on first successful environment build,
# then record the resolved lockfile hash in docs/DECISIONS.md (REPRODUCIBILITY.md).
torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.4
matplotlib>=3.8
pyyaml>=6.0
tqdm>=4.66
# dev
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4

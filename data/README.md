# Data (git-ignored)

Raw + processed Galaxy Zoo artifacts live here. Nothing in this directory is committed.
Provenance, schema, and the frozen split manifest are specified in `tasks/M1-data.md` and recorded in `docs/DECISIONS.md`.
